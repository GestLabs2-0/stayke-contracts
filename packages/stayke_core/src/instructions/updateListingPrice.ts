/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findUserProfilePda } from "../pdas";
import { STAYKE_CORE_PROGRAM_ADDRESS } from "../programs";

export const UPDATE_LISTING_PRICE_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([103, 80, 184, 80, 159, 24, 94, 138]);

export function getUpdateListingPriceDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    UPDATE_LISTING_PRICE_DISCRIMINATOR,
  );
}

export type UpdateListingPriceInstruction<
  TProgram extends string = typeof STAYKE_CORE_PROGRAM_ADDRESS,
  TAccountUserProfile extends string | AccountMeta<string> = string,
  TAccountListing extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountPayer extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountUserProfile extends string
        ? ReadonlyAccount<TAccountUserProfile>
        : TAccountUserProfile,
      TAccountListing extends string
        ? WritableAccount<TAccountListing>
        : TAccountListing,
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> &
            AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      ...TRemainingAccounts,
    ]
  >;

export type UpdateListingPriceInstructionData = {
  discriminator: ReadonlyUint8Array;
  price: bigint;
};

export type UpdateListingPriceInstructionDataArgs = { price: number | bigint };

export function getUpdateListingPriceInstructionDataEncoder(): FixedSizeEncoder<UpdateListingPriceInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["price", getU64Encoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: UPDATE_LISTING_PRICE_DISCRIMINATOR,
    }),
  );
}

export function getUpdateListingPriceInstructionDataDecoder(): FixedSizeDecoder<UpdateListingPriceInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["price", getU64Decoder()],
  ]);
}

export function getUpdateListingPriceInstructionDataCodec(): FixedSizeCodec<
  UpdateListingPriceInstructionDataArgs,
  UpdateListingPriceInstructionData
> {
  return combineCodec(
    getUpdateListingPriceInstructionDataEncoder(),
    getUpdateListingPriceInstructionDataDecoder(),
  );
}

export type UpdateListingPriceAsyncInput<
  TAccountUserProfile extends string = string,
  TAccountListing extends string = string,
  TAccountAuthority extends string = string,
  TAccountPayer extends string = string,
> = {
  userProfile?: Address<TAccountUserProfile>;
  listing: Address<TAccountListing>;
  authority: TransactionSigner<TAccountAuthority>;
  payer: TransactionSigner<TAccountPayer>;
  price: UpdateListingPriceInstructionDataArgs["price"];
};

export async function getUpdateListingPriceInstructionAsync<
  TAccountUserProfile extends string,
  TAccountListing extends string,
  TAccountAuthority extends string,
  TAccountPayer extends string,
  TProgramAddress extends Address = typeof STAYKE_CORE_PROGRAM_ADDRESS,
>(
  input: UpdateListingPriceAsyncInput<
    TAccountUserProfile,
    TAccountListing,
    TAccountAuthority,
    TAccountPayer
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  UpdateListingPriceInstruction<
    TProgramAddress,
    TAccountUserProfile,
    TAccountListing,
    TAccountAuthority,
    TAccountPayer
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? STAYKE_CORE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    userProfile: { value: input.userProfile ?? null, isWritable: false },
    listing: { value: input.listing ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: false },
    payer: { value: input.payer ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.userProfile.value) {
    accounts.userProfile.value = await findUserProfilePda({
      authority: getAddressFromResolvedInstructionAccount(
        "authority",
        accounts.authority.value,
      ),
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("userProfile", accounts.userProfile),
      getAccountMeta("listing", accounts.listing),
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("payer", accounts.payer),
    ],
    data: getUpdateListingPriceInstructionDataEncoder().encode(
      args as UpdateListingPriceInstructionDataArgs,
    ),
    programAddress,
  } as UpdateListingPriceInstruction<
    TProgramAddress,
    TAccountUserProfile,
    TAccountListing,
    TAccountAuthority,
    TAccountPayer
  >);
}

export type UpdateListingPriceInput<
  TAccountUserProfile extends string = string,
  TAccountListing extends string = string,
  TAccountAuthority extends string = string,
  TAccountPayer extends string = string,
> = {
  userProfile: Address<TAccountUserProfile>;
  listing: Address<TAccountListing>;
  authority: TransactionSigner<TAccountAuthority>;
  payer: TransactionSigner<TAccountPayer>;
  price: UpdateListingPriceInstructionDataArgs["price"];
};

export function getUpdateListingPriceInstruction<
  TAccountUserProfile extends string,
  TAccountListing extends string,
  TAccountAuthority extends string,
  TAccountPayer extends string,
  TProgramAddress extends Address = typeof STAYKE_CORE_PROGRAM_ADDRESS,
>(
  input: UpdateListingPriceInput<
    TAccountUserProfile,
    TAccountListing,
    TAccountAuthority,
    TAccountPayer
  >,
  config?: { programAddress?: TProgramAddress },
): UpdateListingPriceInstruction<
  TProgramAddress,
  TAccountUserProfile,
  TAccountListing,
  TAccountAuthority,
  TAccountPayer
> {
  // Program address.
  const programAddress = config?.programAddress ?? STAYKE_CORE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    userProfile: { value: input.userProfile ?? null, isWritable: false },
    listing: { value: input.listing ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: false },
    payer: { value: input.payer ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("userProfile", accounts.userProfile),
      getAccountMeta("listing", accounts.listing),
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("payer", accounts.payer),
    ],
    data: getUpdateListingPriceInstructionDataEncoder().encode(
      args as UpdateListingPriceInstructionDataArgs,
    ),
    programAddress,
  } as UpdateListingPriceInstruction<
    TProgramAddress,
    TAccountUserProfile,
    TAccountListing,
    TAccountAuthority,
    TAccountPayer
  >);
}

export type ParsedUpdateListingPriceInstruction<
  TProgram extends string = typeof STAYKE_CORE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    userProfile: TAccountMetas[0];
    listing: TAccountMetas[1];
    authority: TAccountMetas[2];
    payer: TAccountMetas[3];
  };
  data: UpdateListingPriceInstructionData;
};

export function parseUpdateListingPriceInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedUpdateListingPriceInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      userProfile: getNextAccount(),
      listing: getNextAccount(),
      authority: getNextAccount(),
      payer: getNextAccount(),
    },
    data: getUpdateListingPriceInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
