/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertIsInstructionWithAccounts,
  containsBytes,
  extendClient,
  fixEncoderSize,
  getBytesEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__FAILED_TO_IDENTIFY_ACCOUNT,
  SOLANA_ERROR__PROGRAM_CLIENTS__FAILED_TO_IDENTIFY_INSTRUCTION,
  SOLANA_ERROR__PROGRAM_CLIENTS__UNRECOGNIZED_INSTRUCTION_TYPE,
  SolanaError,
  type Address,
  type ClientWithRpc,
  type ClientWithTransactionPlanning,
  type ClientWithTransactionSending,
  type GetAccountInfoApi,
  type GetMultipleAccountsApi,
  type Instruction,
  type InstructionWithData,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  addSelfFetchFunctions,
  addSelfPlanAndSendFunctions,
  type SelfFetchFunctions,
  type SelfPlanAndSendFunctions,
} from "@solana/program-client-core";
import {
  getGlobalConfigCodec,
  type GlobalConfig,
  type GlobalConfigArgs,
} from "../accounts";
import {
  getInitializeConfigInstructionAsync,
  getWithdrawFeesInstructionAsync,
  parseInitializeConfigInstruction,
  parseWithdrawFeesInstruction,
  type InitializeConfigAsyncInput,
  type ParsedInitializeConfigInstruction,
  type ParsedWithdrawFeesInstruction,
  type WithdrawFeesAsyncInput,
} from "../instructions";
import {
  findGlobalConfigPda,
  findPlatformVaultPda,
  findPlatformVaultPdaPda,
} from "../pdas";

export const STAYKE_CONFIG_PROGRAM_ADDRESS =
  "9ESE5Ztpr8zWbLyXCyiB5QqcjxHghotT8zqJxD2S3zaT" as Address<"9ESE5Ztpr8zWbLyXCyiB5QqcjxHghotT8zqJxD2S3zaT">;

export enum StaykeConfigAccount {
  GlobalConfig,
}

export function identifyStaykeConfigAccount(
  account: { data: ReadonlyUint8Array } | ReadonlyUint8Array,
): StaykeConfigAccount {
  const data = "data" in account ? account.data : account;
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([149, 8, 156, 202, 160, 252, 176, 217]),
      ),
      0,
    )
  ) {
    return StaykeConfigAccount.GlobalConfig;
  }
  throw new SolanaError(
    SOLANA_ERROR__PROGRAM_CLIENTS__FAILED_TO_IDENTIFY_ACCOUNT,
    { accountData: data, programName: "staykeConfig" },
  );
}

export enum StaykeConfigInstruction {
  InitializeConfig,
  WithdrawFees,
}

export function identifyStaykeConfigInstruction(
  instruction: { data: ReadonlyUint8Array } | ReadonlyUint8Array,
): StaykeConfigInstruction {
  const data = "data" in instruction ? instruction.data : instruction;
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([208, 127, 21, 1, 194, 190, 196, 70]),
      ),
      0,
    )
  ) {
    return StaykeConfigInstruction.InitializeConfig;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([198, 212, 171, 109, 144, 215, 174, 89]),
      ),
      0,
    )
  ) {
    return StaykeConfigInstruction.WithdrawFees;
  }
  throw new SolanaError(
    SOLANA_ERROR__PROGRAM_CLIENTS__FAILED_TO_IDENTIFY_INSTRUCTION,
    { instructionData: data, programName: "staykeConfig" },
  );
}

export type ParsedStaykeConfigInstruction<
  TProgram extends string = "9ESE5Ztpr8zWbLyXCyiB5QqcjxHghotT8zqJxD2S3zaT",
> =
  | ({
      instructionType: StaykeConfigInstruction.InitializeConfig;
    } & ParsedInitializeConfigInstruction<TProgram>)
  | ({
      instructionType: StaykeConfigInstruction.WithdrawFees;
    } & ParsedWithdrawFeesInstruction<TProgram>);

export function parseStaykeConfigInstruction<TProgram extends string>(
  instruction: Instruction<TProgram> & InstructionWithData<ReadonlyUint8Array>,
): ParsedStaykeConfigInstruction<TProgram> {
  const instructionType = identifyStaykeConfigInstruction(instruction);
  switch (instructionType) {
    case StaykeConfigInstruction.InitializeConfig: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: StaykeConfigInstruction.InitializeConfig,
        ...parseInitializeConfigInstruction(instruction),
      };
    }
    case StaykeConfigInstruction.WithdrawFees: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: StaykeConfigInstruction.WithdrawFees,
        ...parseWithdrawFeesInstruction(instruction),
      };
    }
    default:
      throw new SolanaError(
        SOLANA_ERROR__PROGRAM_CLIENTS__UNRECOGNIZED_INSTRUCTION_TYPE,
        {
          instructionType: instructionType as string,
          programName: "staykeConfig",
        },
      );
  }
}

export type StaykeConfigPlugin = {
  accounts: StaykeConfigPluginAccounts;
  instructions: StaykeConfigPluginInstructions;
  pdas: StaykeConfigPluginPdas;
};

export type StaykeConfigPluginAccounts = {
  globalConfig: ReturnType<typeof getGlobalConfigCodec> &
    SelfFetchFunctions<GlobalConfigArgs, GlobalConfig>;
};

export type StaykeConfigPluginInstructions = {
  initializeConfig: (
    input: InitializeConfigAsyncInput,
  ) => ReturnType<typeof getInitializeConfigInstructionAsync> &
    SelfPlanAndSendFunctions;
  withdrawFees: (
    input: WithdrawFeesAsyncInput,
  ) => ReturnType<typeof getWithdrawFeesInstructionAsync> &
    SelfPlanAndSendFunctions;
};

export type StaykeConfigPluginPdas = {
  globalConfig: typeof findGlobalConfigPda;
  platformVaultPda: typeof findPlatformVaultPdaPda;
  platformVault: typeof findPlatformVaultPda;
};

export type StaykeConfigPluginRequirements = ClientWithRpc<
  GetAccountInfoApi & GetMultipleAccountsApi
> &
  ClientWithTransactionPlanning &
  ClientWithTransactionSending;

export function staykeConfigProgram() {
  return <T extends StaykeConfigPluginRequirements>(
    client: T,
  ): Omit<T, "staykeConfig"> & { staykeConfig: StaykeConfigPlugin } => {
    return extendClient(client, {
      staykeConfig: <StaykeConfigPlugin>{
        accounts: {
          globalConfig: addSelfFetchFunctions(client, getGlobalConfigCodec()),
        },
        instructions: {
          initializeConfig: (input) =>
            addSelfPlanAndSendFunctions(
              client,
              getInitializeConfigInstructionAsync(input),
            ),
          withdrawFees: (input) =>
            addSelfPlanAndSendFunctions(
              client,
              getWithdrawFeesInstructionAsync(input),
            ),
        },
        pdas: {
          globalConfig: findGlobalConfigPda,
          platformVaultPda: findPlatformVaultPdaPda,
          platformVault: findPlatformVaultPda,
        },
      },
    });
  };
}
