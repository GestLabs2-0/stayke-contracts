/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findDisputePda } from "../pdas";
import { STAYKE_DISPUTES_PROGRAM_ADDRESS } from "../programs";

export const LINK_EVIDENTE_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  130, 247, 163, 67, 46, 217, 20, 174,
]);

export function getLinkEvidenteDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    LINK_EVIDENTE_DISCRIMINATOR,
  );
}

export type LinkEvidenteInstruction<
  TProgram extends string = typeof STAYKE_DISPUTES_PROGRAM_ADDRESS,
  TAccountSigner extends string | AccountMeta<string> = string,
  TAccountDispute extends string | AccountMeta<string> = string,
  TAccountBooking extends string | AccountMeta<string> = string,
  TAccountUserProfile extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSigner extends string
        ? ReadonlySignerAccount<TAccountSigner> &
            AccountSignerMeta<TAccountSigner>
        : TAccountSigner,
      TAccountDispute extends string
        ? ReadonlyAccount<TAccountDispute>
        : TAccountDispute,
      TAccountBooking extends string
        ? ReadonlyAccount<TAccountBooking>
        : TAccountBooking,
      TAccountUserProfile extends string
        ? ReadonlyAccount<TAccountUserProfile>
        : TAccountUserProfile,
      ...TRemainingAccounts,
    ]
  >;

export type LinkEvidenteInstructionData = {
  discriminator: ReadonlyUint8Array;
  evidence: ReadonlyUint8Array;
};

export type LinkEvidenteInstructionDataArgs = { evidence: ReadonlyUint8Array };

export function getLinkEvidenteInstructionDataEncoder(): FixedSizeEncoder<LinkEvidenteInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["evidence", fixEncoderSize(getBytesEncoder(), 32)],
    ]),
    (value) => ({ ...value, discriminator: LINK_EVIDENTE_DISCRIMINATOR }),
  );
}

export function getLinkEvidenteInstructionDataDecoder(): FixedSizeDecoder<LinkEvidenteInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["evidence", fixDecoderSize(getBytesDecoder(), 32)],
  ]);
}

export function getLinkEvidenteInstructionDataCodec(): FixedSizeCodec<
  LinkEvidenteInstructionDataArgs,
  LinkEvidenteInstructionData
> {
  return combineCodec(
    getLinkEvidenteInstructionDataEncoder(),
    getLinkEvidenteInstructionDataDecoder(),
  );
}

export type LinkEvidenteAsyncInput<
  TAccountSigner extends string = string,
  TAccountDispute extends string = string,
  TAccountBooking extends string = string,
  TAccountUserProfile extends string = string,
> = {
  signer: TransactionSigner<TAccountSigner>;
  dispute?: Address<TAccountDispute>;
  booking: Address<TAccountBooking>;
  userProfile?: Address<TAccountUserProfile>;
  evidence: LinkEvidenteInstructionDataArgs["evidence"];
};

export async function getLinkEvidenteInstructionAsync<
  TAccountSigner extends string,
  TAccountDispute extends string,
  TAccountBooking extends string,
  TAccountUserProfile extends string,
  TProgramAddress extends Address = typeof STAYKE_DISPUTES_PROGRAM_ADDRESS,
>(
  input: LinkEvidenteAsyncInput<
    TAccountSigner,
    TAccountDispute,
    TAccountBooking,
    TAccountUserProfile
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  LinkEvidenteInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountDispute,
    TAccountBooking,
    TAccountUserProfile
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? STAYKE_DISPUTES_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: false },
    dispute: { value: input.dispute ?? null, isWritable: false },
    booking: { value: input.booking ?? null, isWritable: false },
    userProfile: { value: input.userProfile ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.dispute.value) {
    accounts.dispute.value = await findDisputePda({
      booking: getAddressFromResolvedInstructionAccount(
        "booking",
        accounts.booking.value,
      ),
    });
  }
  if (!accounts.userProfile.value) {
    accounts.userProfile.value = await getProgramDerivedAddress({
      programAddress:
        "2u1JrVasLvuGR5s3n84p5yaitHU2PGa8VjWZ7P2Eescm" as Address<"2u1JrVasLvuGR5s3n84p5yaitHU2PGa8VjWZ7P2Eescm">,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([
            117, 115, 101, 114, 95, 112, 114, 111, 102, 105, 108, 101,
          ]),
        ),
        getAddressEncoder().encode(
          getAddressFromResolvedInstructionAccount(
            "signer",
            accounts.signer.value,
          ),
        ),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("signer", accounts.signer),
      getAccountMeta("dispute", accounts.dispute),
      getAccountMeta("booking", accounts.booking),
      getAccountMeta("userProfile", accounts.userProfile),
    ],
    data: getLinkEvidenteInstructionDataEncoder().encode(
      args as LinkEvidenteInstructionDataArgs,
    ),
    programAddress,
  } as LinkEvidenteInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountDispute,
    TAccountBooking,
    TAccountUserProfile
  >);
}

export type LinkEvidenteInput<
  TAccountSigner extends string = string,
  TAccountDispute extends string = string,
  TAccountBooking extends string = string,
  TAccountUserProfile extends string = string,
> = {
  signer: TransactionSigner<TAccountSigner>;
  dispute: Address<TAccountDispute>;
  booking: Address<TAccountBooking>;
  userProfile: Address<TAccountUserProfile>;
  evidence: LinkEvidenteInstructionDataArgs["evidence"];
};

export function getLinkEvidenteInstruction<
  TAccountSigner extends string,
  TAccountDispute extends string,
  TAccountBooking extends string,
  TAccountUserProfile extends string,
  TProgramAddress extends Address = typeof STAYKE_DISPUTES_PROGRAM_ADDRESS,
>(
  input: LinkEvidenteInput<
    TAccountSigner,
    TAccountDispute,
    TAccountBooking,
    TAccountUserProfile
  >,
  config?: { programAddress?: TProgramAddress },
): LinkEvidenteInstruction<
  TProgramAddress,
  TAccountSigner,
  TAccountDispute,
  TAccountBooking,
  TAccountUserProfile
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? STAYKE_DISPUTES_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: false },
    dispute: { value: input.dispute ?? null, isWritable: false },
    booking: { value: input.booking ?? null, isWritable: false },
    userProfile: { value: input.userProfile ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("signer", accounts.signer),
      getAccountMeta("dispute", accounts.dispute),
      getAccountMeta("booking", accounts.booking),
      getAccountMeta("userProfile", accounts.userProfile),
    ],
    data: getLinkEvidenteInstructionDataEncoder().encode(
      args as LinkEvidenteInstructionDataArgs,
    ),
    programAddress,
  } as LinkEvidenteInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountDispute,
    TAccountBooking,
    TAccountUserProfile
  >);
}

export type ParsedLinkEvidenteInstruction<
  TProgram extends string = typeof STAYKE_DISPUTES_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    signer: TAccountMetas[0];
    dispute: TAccountMetas[1];
    booking: TAccountMetas[2];
    userProfile: TAccountMetas[3];
  };
  data: LinkEvidenteInstructionData;
};

export function parseLinkEvidenteInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedLinkEvidenteInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      signer: getNextAccount(),
      dispute: getNextAccount(),
      booking: getNextAccount(),
      userProfile: getNextAccount(),
    },
    data: getLinkEvidenteInstructionDataDecoder().decode(instruction.data),
  };
}
