/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { STAYKE_DISPUTES_PROGRAM_ADDRESS } from "../programs";

export const ESCALATE_DISPUTE_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([129, 70, 111, 75, 130, 29, 225, 35]);

export function getEscalateDisputeDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    ESCALATE_DISPUTE_DISCRIMINATOR,
  );
}

export type EscalateDisputeInstruction<
  TProgram extends string = typeof STAYKE_DISPUTES_PROGRAM_ADDRESS,
  TAccountDispute extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountDispute extends string
        ? WritableAccount<TAccountDispute>
        : TAccountDispute,
      ...TRemainingAccounts,
    ]
  >;

export type EscalateDisputeInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type EscalateDisputeInstructionDataArgs = {};

export function getEscalateDisputeInstructionDataEncoder(): FixedSizeEncoder<EscalateDisputeInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: ESCALATE_DISPUTE_DISCRIMINATOR }),
  );
}

export function getEscalateDisputeInstructionDataDecoder(): FixedSizeDecoder<EscalateDisputeInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getEscalateDisputeInstructionDataCodec(): FixedSizeCodec<
  EscalateDisputeInstructionDataArgs,
  EscalateDisputeInstructionData
> {
  return combineCodec(
    getEscalateDisputeInstructionDataEncoder(),
    getEscalateDisputeInstructionDataDecoder(),
  );
}

export type EscalateDisputeInput<TAccountDispute extends string = string> = {
  dispute: Address<TAccountDispute>;
};

export function getEscalateDisputeInstruction<
  TAccountDispute extends string,
  TProgramAddress extends Address = typeof STAYKE_DISPUTES_PROGRAM_ADDRESS,
>(
  input: EscalateDisputeInput<TAccountDispute>,
  config?: { programAddress?: TProgramAddress },
): EscalateDisputeInstruction<TProgramAddress, TAccountDispute> {
  // Program address.
  const programAddress =
    config?.programAddress ?? STAYKE_DISPUTES_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    dispute: { value: input.dispute ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [getAccountMeta("dispute", accounts.dispute)],
    data: getEscalateDisputeInstructionDataEncoder().encode({}),
    programAddress,
  } as EscalateDisputeInstruction<TProgramAddress, TAccountDispute>);
}

export type ParsedEscalateDisputeInstruction<
  TProgram extends string = typeof STAYKE_DISPUTES_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    dispute: TAccountMetas[0];
  };
  data: EscalateDisputeInstructionData;
};

export function parseEscalateDisputeInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedEscalateDisputeInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 1) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 1,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { dispute: getNextAccount() },
    data: getEscalateDisputeInstructionDataDecoder().decode(instruction.data),
  };
}
