/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findDisputeConfigPda } from "../pdas";
import { STAYKE_DISPUTES_PROGRAM_ADDRESS } from "../programs";

export const DELETE_ADMIN_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  185, 158, 127, 54, 59, 60, 205, 164,
]);

export function getDeleteAdminDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    DELETE_ADMIN_DISCRIMINATOR,
  );
}

export type DeleteAdminInstruction<
  TProgram extends string = typeof STAYKE_DISPUTES_PROGRAM_ADDRESS,
  TAccountAdmin extends string | AccountMeta<string> = string,
  TAccountDisputeConfig extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAdmin extends string
        ? ReadonlySignerAccount<TAccountAdmin> &
            AccountSignerMeta<TAccountAdmin>
        : TAccountAdmin,
      TAccountDisputeConfig extends string
        ? WritableAccount<TAccountDisputeConfig>
        : TAccountDisputeConfig,
      ...TRemainingAccounts,
    ]
  >;

export type DeleteAdminInstructionData = {
  discriminator: ReadonlyUint8Array;
  admin: Address;
};

export type DeleteAdminInstructionDataArgs = { admin: Address };

export function getDeleteAdminInstructionDataEncoder(): FixedSizeEncoder<DeleteAdminInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["admin", getAddressEncoder()],
    ]),
    (value) => ({ ...value, discriminator: DELETE_ADMIN_DISCRIMINATOR }),
  );
}

export function getDeleteAdminInstructionDataDecoder(): FixedSizeDecoder<DeleteAdminInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["admin", getAddressDecoder()],
  ]);
}

export function getDeleteAdminInstructionDataCodec(): FixedSizeCodec<
  DeleteAdminInstructionDataArgs,
  DeleteAdminInstructionData
> {
  return combineCodec(
    getDeleteAdminInstructionDataEncoder(),
    getDeleteAdminInstructionDataDecoder(),
  );
}

export type DeleteAdminAsyncInput<
  TAccountAdmin extends string = string,
  TAccountDisputeConfig extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  disputeConfig?: Address<TAccountDisputeConfig>;
  adminArg: DeleteAdminInstructionDataArgs["admin"];
};

export async function getDeleteAdminInstructionAsync<
  TAccountAdmin extends string,
  TAccountDisputeConfig extends string,
  TProgramAddress extends Address = typeof STAYKE_DISPUTES_PROGRAM_ADDRESS,
>(
  input: DeleteAdminAsyncInput<TAccountAdmin, TAccountDisputeConfig>,
  config?: { programAddress?: TProgramAddress },
): Promise<
  DeleteAdminInstruction<TProgramAddress, TAccountAdmin, TAccountDisputeConfig>
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? STAYKE_DISPUTES_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: false },
    disputeConfig: { value: input.disputeConfig ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input, admin: input.adminArg };

  // Resolve default values.
  if (!accounts.disputeConfig.value) {
    accounts.disputeConfig.value = await findDisputeConfigPda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("admin", accounts.admin),
      getAccountMeta("disputeConfig", accounts.disputeConfig),
    ],
    data: getDeleteAdminInstructionDataEncoder().encode(
      args as DeleteAdminInstructionDataArgs,
    ),
    programAddress,
  } as DeleteAdminInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountDisputeConfig
  >);
}

export type DeleteAdminInput<
  TAccountAdmin extends string = string,
  TAccountDisputeConfig extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  disputeConfig: Address<TAccountDisputeConfig>;
  adminArg: DeleteAdminInstructionDataArgs["admin"];
};

export function getDeleteAdminInstruction<
  TAccountAdmin extends string,
  TAccountDisputeConfig extends string,
  TProgramAddress extends Address = typeof STAYKE_DISPUTES_PROGRAM_ADDRESS,
>(
  input: DeleteAdminInput<TAccountAdmin, TAccountDisputeConfig>,
  config?: { programAddress?: TProgramAddress },
): DeleteAdminInstruction<
  TProgramAddress,
  TAccountAdmin,
  TAccountDisputeConfig
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? STAYKE_DISPUTES_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: false },
    disputeConfig: { value: input.disputeConfig ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input, admin: input.adminArg };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("admin", accounts.admin),
      getAccountMeta("disputeConfig", accounts.disputeConfig),
    ],
    data: getDeleteAdminInstructionDataEncoder().encode(
      args as DeleteAdminInstructionDataArgs,
    ),
    programAddress,
  } as DeleteAdminInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountDisputeConfig
  >);
}

export type ParsedDeleteAdminInstruction<
  TProgram extends string = typeof STAYKE_DISPUTES_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    admin: TAccountMetas[0];
    disputeConfig: TAccountMetas[1];
  };
  data: DeleteAdminInstructionData;
};

export function parseDeleteAdminInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedDeleteAdminInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { admin: getNextAccount(), disputeConfig: getNextAccount() },
    data: getDeleteAdminInstructionDataDecoder().decode(instruction.data),
  };
}
