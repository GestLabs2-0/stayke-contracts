/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getOptionDecoder,
  getOptionEncoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type Codec,
  type Decoder,
  type EncodedAccount,
  type Encoder,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type Option,
  type OptionOrNullable,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getBookingStatusDecoder,
  getBookingStatusEncoder,
  getDisputeOutcomeDecoder,
  getDisputeOutcomeEncoder,
  getDisputePartyDecoder,
  getDisputePartyEncoder,
  getDisputeStateDecoder,
  getDisputeStateEncoder,
  type BookingStatus,
  type BookingStatusArgs,
  type DisputeOutcome,
  type DisputeOutcomeArgs,
  type DisputeParty,
  type DisputePartyArgs,
  type DisputeState,
  type DisputeStateArgs,
} from "../types";

export const DISPUTE_ACCOUNT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array(
  [237, 70, 91, 63, 81, 74, 45, 43],
);

export function getDisputeAccountDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    DISPUTE_ACCOUNT_DISCRIMINATOR,
  );
}

export type DisputeAccount = {
  discriminator: ReadonlyUint8Array;
  booking: Address;
  openedBy: DisputeParty;
  state: DisputeState;
  openedAt: bigint;
  guestEvidence: Option<ReadonlyUint8Array>;
  hostEvidence: Option<ReadonlyUint8Array>;
  outcome: Option<DisputeOutcome>;
  originalBookingStatus: BookingStatus;
  bump: number;
};

export type DisputeAccountArgs = {
  booking: Address;
  openedBy: DisputePartyArgs;
  state: DisputeStateArgs;
  openedAt: number | bigint;
  guestEvidence: OptionOrNullable<ReadonlyUint8Array>;
  hostEvidence: OptionOrNullable<ReadonlyUint8Array>;
  outcome: OptionOrNullable<DisputeOutcomeArgs>;
  originalBookingStatus: BookingStatusArgs;
  bump: number;
};

/** Gets the encoder for {@link DisputeAccountArgs} account data. */
export function getDisputeAccountEncoder(): Encoder<DisputeAccountArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["booking", getAddressEncoder()],
      ["openedBy", getDisputePartyEncoder()],
      ["state", getDisputeStateEncoder()],
      ["openedAt", getI64Encoder()],
      [
        "guestEvidence",
        getOptionEncoder(fixEncoderSize(getBytesEncoder(), 32)),
      ],
      ["hostEvidence", getOptionEncoder(fixEncoderSize(getBytesEncoder(), 32))],
      ["outcome", getOptionEncoder(getDisputeOutcomeEncoder())],
      ["originalBookingStatus", getBookingStatusEncoder()],
      ["bump", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: DISPUTE_ACCOUNT_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link DisputeAccount} account data. */
export function getDisputeAccountDecoder(): Decoder<DisputeAccount> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["booking", getAddressDecoder()],
    ["openedBy", getDisputePartyDecoder()],
    ["state", getDisputeStateDecoder()],
    ["openedAt", getI64Decoder()],
    ["guestEvidence", getOptionDecoder(fixDecoderSize(getBytesDecoder(), 32))],
    ["hostEvidence", getOptionDecoder(fixDecoderSize(getBytesDecoder(), 32))],
    ["outcome", getOptionDecoder(getDisputeOutcomeDecoder())],
    ["originalBookingStatus", getBookingStatusDecoder()],
    ["bump", getU8Decoder()],
  ]);
}

/** Gets the codec for {@link DisputeAccount} account data. */
export function getDisputeAccountCodec(): Codec<
  DisputeAccountArgs,
  DisputeAccount
> {
  return combineCodec(getDisputeAccountEncoder(), getDisputeAccountDecoder());
}

export function decodeDisputeAccount<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<DisputeAccount, TAddress>;
export function decodeDisputeAccount<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<DisputeAccount, TAddress>;
export function decodeDisputeAccount<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<DisputeAccount, TAddress> | MaybeAccount<DisputeAccount, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getDisputeAccountDecoder(),
  );
}

export async function fetchDisputeAccount<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<DisputeAccount, TAddress>> {
  const maybeAccount = await fetchMaybeDisputeAccount(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeDisputeAccount<
  TAddress extends string = string,
>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<DisputeAccount, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeDisputeAccount(maybeAccount);
}

export async function fetchAllDisputeAccount(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<DisputeAccount>[]> {
  const maybeAccounts = await fetchAllMaybeDisputeAccount(
    rpc,
    addresses,
    config,
  );
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeDisputeAccount(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<DisputeAccount>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) =>
    decodeDisputeAccount(maybeAccount),
  );
}
